@using Nop.Plugin.Baramjk.PhotoPlatform.Areas.Admin.Models.Subjects
@using Nop.Web.Framework.Models.DataTables
@model Nop.Plugin.Baramjk.PhotoPlatform.Areas.Admin.Models.Subjects.SubjectSearchModel

@{
    ViewBag.PageTitle = T("Nop.Plugin.Baramjk.PhotoPlatform.ContactUs.Admin.Subjects.Title").Text;
    Html.SetActiveMenuItemSystemName($"{SystemName}.ContactUsSubjects.List");
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-header clearfix">
    <h1 class="float-left">
        @T("Nop.Plugin.Baramjk.PhotoPlatform.ContactUs.Admin.Subjects.Title")
    </h1>
    <div class="float-right">
        <a asp-action="Create" asp-controller="ContactUsSubject" class="btn btn-primary">
            <i class="fas fa-plus-square"></i>
            @T("Admin.Common.AddNew")
        </a>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        <div class="form-horizontal">
            <div class="card card-default">
                <div class="card-body">
                    @await Html.PartialAsync("Table", new DataTablesModel
                    {
                        Name = "subjects-grid",
                        UrlRead = new DataUrl("List", "ContactUsSubject", null),
                        UrlUpdate = new DataUrl("Edit", "ContactUsSubject", null),
                        UrlDelete = new DataUrl("DeleteById", "ContactUsSubject", null),
                        Length = Model.PageSize,
                        LengthMenu = Model.AvailablePageSizes,
                        ColumnCollection = new List<ColumnProperty>
                        {
                            new(nameof(SubjectViewModel.Name))
                            {
                                Title = T("Nop.Plugin.Baramjk.PhotoPlatform.ContactUs.Admin.Subjects.Name").Text,
                            },
                            
                            new(nameof(SubjectViewModel.Id))
                            {
                                Title = T("Admin.Common.Edit").Text,
                                Width = "50",
                                ClassName = NopColumnClassDefaults.Button,
                                Render = new RenderButtonEdit(new DataUrl("Edit"))
                            },
                            new(nameof(SubjectViewModel.Id))
                            {
                                Title = T("Admin.Common.Delete").Text,
                                Width = "50",
                                ClassName = NopColumnClassDefaults.Button,
                                Render = new RenderButtonRemove(T("Admin.Common.Delete").Text)
                            }
                        }
                    })
                </div>
            </div>
        </div>
    </div>
</section>